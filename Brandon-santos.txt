https://codepen.io/Brandonx1123/pen/wvWJbKz

<!-- Questions to Submit in today's file first-lastname.txt --> 
<!--
Copy the questions below into the first-lastname.txt file on your github repo - answer each question and then push your changes. 

    1. What is Semantic HTML?

 it is html that gives our content meaning.

    2. What is HTML used for?

 HTML is used for displaying data in structures, headings and paragraphs to a webpage.

    3. What is an attribute and where do we put it?

 An attribute is the specific classification of an HTML element and it belongs in HTML formatting.

    4. What is the h1 tag used for? How many times should I use it on a page? 

An H1 tag is used for the main heading on your webpage. It will be the biggest heading throughout the page keep in mind. You should use it only ONCE per page as good practice.

5. Named attribute two tags that have requires

<a> and <img> are two tags.

    6. What do we put in the head of our HTML document? 
need <header> <h1>
    7. What is an id?
 
an id is a classification for an element
 
    8. What elements can I add an id to?
 
you can add an id to every element but only once.

    9. How many times can I use the same id on a page?
 
you can only use id once per page
 
    10. What is a class? 

a is a classification tag for an element you can use multiple times. 

    11. What elements can I add a class to?
 
All elements can be given a class.

    12. How many times can I use the same class on a page? 

You can have multiple the same class per page

    13. How do I get my link to open in a new tab?

You must use the command within the link of target="_blank"

    14. What is the alt attribute in the image tag used for? 

the alt attribute in an image tag is to give a brief explantion of what the image is.

    15. How do I reference an id?

to reference in ccs you need to do a # and the name of the id tag.

    16. What is the difference between a section and a div

section groups text by meaning and a div has no limitations and can use it anywhere on the html

    17. What is CSS used for? 

is used to style a HTML webpage

    18. How to we select an element? Example - every h2 on the page

name of the element and {}

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
   
difference between a class and an id is that a class you can use many times throughout a page but an id you can only use it to call for an element once.

<section class="cool kids">
 <div id="great people"

20. How do we select classes in CSS?

you have to use a (( . )) + name of class 

    21. How do we select a p element with a single class of “human”?
.human p {}

    22. What is a parent child selector? When would this be useful? 

parent/ child selector is that everything within a certain element or pertains to an element is its child. the parent of the child is the main element.This is useful is you want to break up certain things within a <section> with <divs>.

    23. How do you select all links within a div with the class of sidebar?

 <div class=sidebar> //// -> Answer  .sidebar .a{} -
<a>

    24. What is a pseudo selector?

the special state of an element such as when u hover over or make it a button etc.

    25. What do we use the change the spacing between lines?

 line-height command should do the job.

    26. What do we use to change the spacing between letters?

this command-
line-height:  

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?

the command is -
text-transform- lowercase;
text-transform- capitalize;

    28. How do I add a 1px border around my div that is dotted and black?

."div class"{
border: 1px dotted black;
}

 29. How do I select everything on the page? 

use an astor (*) and use a curly bracket after.

 30. How do I write a comment in CSS?

/* is used to put a comment on css.

 31. How do I find out what file I am in, when I am using the command line?
 
you must use pwd

    32. Using the command line - how do I see a list of files/folders in my current folder?

Use ls (List) is the command to see the list of everything within a folder. 
 
    33. How do I remove a file via the command line?
rm and file name in git bash 
 Why do I have to be careful with this? 
because this will be gone forever basically
    34. Why should I use version control? 
we should a version control because it allows you to make multiple changes to a file and have different versions. hence "version" control.
    35. How often should I commit to github?

You should commit every 15-20 respectively. Just to be safe

    36. What is the command we would use to push our repo up to github?
 
git -u origin branch name

    37. Walk me through Lambda's git flow. 
fork the copy from lambdas git. then you want to keep a copy of the url for the fork. then go to git bash and find the directory you want to work in then [cd] to the file. always check with pwd and ls to see where u are.then you want to git clone the repo into the folder youre in to work with. then cd to the new repos link(should be above) wihtin ur folder. after this you need to make your own branch. we achieve this by using git checkout -b "first name and last". after this use git add . to make any changes and then use git commit -m "any message about changes" to comment about the changes in ur work. After this you git push -u origin "branch name" back to git. After it is in git all you haveto do is do a pull request.
Stretch Questions

    1. What is the difference between an inline element and a block element? a block element will send the element to a different line. An inline element will keep it on the same line.
    2. What happens when an element is positioned absolutely?  

An element that is positioned absolutely makes it out of the flow of all the other elements. So it will be positioned anywhere where the root element is by on the html page.
 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 

    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default

3 block elements: <h1>, <p>, <div>. 2 inline elements: <span> <a>. 1 inline-block element:<span> (which u can put the display:inline-block element for in css)

    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them?
 
the box model pertains to the boxes of a webpage.So you can put a border around an element and within that border is a box. Within every element techinically is a box. So the box model is that each
